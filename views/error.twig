<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ errorTitle || 'Error' }}</title>
</head>
<body>
    <h1>{{ errorTitle || 'Error Occurred' }}</h1>
    <p>{{ errorMessage || 'An unexpected error happened.' }}</p>
</body>
</html>
